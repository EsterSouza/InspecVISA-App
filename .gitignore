# Environment variables (secrets — nunca commitar)
.env
.env.local
.env.development
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.claude/
supabase/.temp/

# Local backup exports
inspec-visa-backup-*.json

# Local planning/context notes
CONTEXTO_INSPECVISA.md
Inspecao_REDE_SÊNIOR_UNIDADE_COPACABANA_15-05-2026.pdf

# Estoque Regulatorio da Anvisa (fonte de consulta dos Cards 1-4, binario grande)
planilha regulat*anvisa*.xlsx
planilha regulat*ANVISA*.xlsx

# Dumps de produção usados por scripts/ref01-build-inventory.ts (REF-01/REF-02).
# São snapshot, não fonte de verdade — ver o cabeçalho de scripts/ref02-dump.mjs.
scripts/ref01-raw.json
scripts/ref01-legislations.json

# Cópias de trabalho locais (backups de cliente, roteiros antigos) — nunca versionar
_local-nao-versionado/

# Credenciais do tenant de homologação (P360-015) e artefatos do Playwright
.env.homolog
test-results/
playwright-report/

# Prot�tipos: fragmentos gerados s� para publicar como artefato
docs/prototipos/_publish/

# MCP com token pessoal/pago — nao versionar (regra do HANDOFF-FRONTEND)
.mcp.json
